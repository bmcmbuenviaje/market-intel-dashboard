{
  "name": "market-intel-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Market & Sales Intelligence Platform — brand partnerships, ownership graphs, sentiment, macro. Cloudflare Pages + Functions.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
